delete


kubectl delete deployment mysql-deployment --ignore-not-found=true
kubectl delete service mysql-service --ignore-not-found=true

kubectl apply -f k8s/02-mysql-deployment.yaml

kubectl get pods

# Проверьте статус phpMyAdmin
kubectl get pods | grep phpmyadmin

# Если phpMyAdmin не запущен, примените его
kubectl apply -f k8s/04-phpmyadmin-deployment.yaml
kubectl apply -f k8s/05-phpmyadmin-service.yaml

```
# Создаем сервис
kubectl expose deployment phpmyadmin-deployment --type=NodePort --port=80 --target-port=80 --name=phpmyadmin-service

# Проверяем порт
kubectl get svc phpmyadmin-service
```

```
kubectl describe pod data-loader-md4h7
```
```
kubectl create secret generic mysql-secret --from-literal=root-password=root123 --from-literal=user-password=analyst456
```

```
kubectl delete job data-loader --ignore-not-found=true
kubectl apply -f k8s/06-loader-job.yaml
kubectl logs -f job/data-loader
```


```
# 1. Переключаемся на docker окружение Minikube
eval $(minikube docker-env)

# 2. Проверяем, что теперь docker смотрит на Minikube
docker images

# 3. Переходим в папку loader
cd ~/Downloads/labs_cicd/2026/lw_03/loader

# 4. Собираем образ
docker build -t data-loader:latest .

# 5. Проверяем, что образ появился
docker images | grep data-loader
```

```
# Удаляем старый job
kubectl delete job data-loader --ignore-not-found=true

# Создаем новый
kubectl apply -f ../k8s/06-loader-job.yaml

# Смотрим логи
kubectl logs -f job/data-loader
```
